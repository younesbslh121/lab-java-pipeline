name: CI/CD INPT Cloud-DevOps

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  # Étape A : Intégration SonarCloud (Questions A.5, A.8, A.9)
  sonarcloud:
    name: SonarCloud Analysis
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build and analyze
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        # On spécifie l'URL de SonarCloud pour éviter l'erreur localhost:9000
        run: >
          mvn -B verify sonar:sonar 
          -Dsonar.projectKey=younesbslh121_lab-java-pipeline 
          -Dsonar.organization=younesbslh121 
          -Dsonar.host.url=https://sonarcloud.io 
          -Dsonar.qualitygate.wait=true

  # Étape B : Création image Docker (Questions B.1, B.2, B.6)
  docker-build:
    name: Docker Build and Push
    needs: sonarcloud # Bloque le build si SonarCloud échoue (Question A.9)
    runs-on: ubuntu-latest
    # Filtre de branche (Question B.6)
    if: github.ref == 'refs/heads/master'
    
    steps:
      - uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build and Push
        run: |
          docker build -t ghcr.io/${{ github.repository }}:latest .
          docker push ghcr.io/${{ github.repository }}:latest
